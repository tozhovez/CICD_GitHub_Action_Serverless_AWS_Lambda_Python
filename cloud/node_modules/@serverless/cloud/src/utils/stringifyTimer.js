'use strict'

module.exports = (numberOfSeconds) => {
  if (numberOfSeconds === 0) {
    return `less than a second`
  }

  if (numberOfSeconds === 1) {
    return `1 second`
  }

  return `${numberOfSeconds} seconds`
}
